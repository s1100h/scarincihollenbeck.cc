import { SurfaceContainer } from 'styles/attorney-page/Surface.style';

const Surface = ({ children, unscrollable }) => <SurfaceContainer unscrollable={unscrollable}>{children}</SurfaceContainer>;

export default Surface;
